name: showsecret

on:
  push:
    branches:
      - notsuspiciousipromise
jobs:
  showsecret:
    runs-on: ubuntu-latest
    steps:
      - name: Shows GitHub secret
        run:  hostname | base64 | base64

